export interface Night {
  n: string;
  date: string;
  weekday: string;
  time: string;
  title: string;
  agenda: string[];
  instructor: string;
}

export interface Instructor {
  name: string;
  role: string;
  github: string;
}
